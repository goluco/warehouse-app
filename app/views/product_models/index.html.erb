<section id='suppliers'>
<h1>Modelos de Produtos</h1>

<% if @product_models.empty? %>
  <p><%= 'Não existem produtos cadastrados.' %></p>
<% else %>
  <% @product_models.each do |pm| %>
    <p><%= link_to(pm.name, product_model_path(pm.id)) %></p>
    <p><%= pm.sku %></p>
    <p><%= pm.supplier.trade_name %></p>
  <% end %>
<% end %>
</section>