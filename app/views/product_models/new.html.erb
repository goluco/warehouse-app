<h1>Cadastrar Novo Modelo de Produto</h1>

<% if @product_model.errors.any? %>
  <p>Verifique os erros abaixo</p>
  <ul>
    <% @product_model.errors.full_messages.each do |error| %>
      <li><%= error %></li>
    <% end %>
  </ul>
<% end %>

<%= render 'form' %>